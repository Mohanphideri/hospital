name: hospital_patient_app
description: Patient mobile application for the Hospital Management System.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  flutter_secure_storage: ^9.2.4
  socket_io_client: ^3.0.2
  intl: ^0.20.2

flutter:
  uses-material-design: true
